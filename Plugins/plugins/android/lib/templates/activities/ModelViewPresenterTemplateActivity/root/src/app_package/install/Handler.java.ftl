package ${packageName}.install;


import com.horical.hrc7.lib_base.base.BaseHandler;
import ${packageName}.I${className}Serve;
import ${packageName}.I${className}View;

public class ${className}Handler extends BaseHandler<I${className}View, I${className}Serve> implements I${className}View.Handler, I${className}Serve.Handler {
    @Override
    protected void onInit() {
	
    }
}