package ${packageName}.install;

import android.content.Intent;
import com.horical.hrc7.lib_base.base.BaseServe;
import ${packageName}.I${className}Serve;

public class ${className}Serve extends BaseServe<I${className}Serve.Handler> implements I${className}Serve {
    
    @Override
    protected void onInit(Intent intent) {
		
    }
}